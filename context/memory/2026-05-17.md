# 2026-05-17

#### Session 1
**Goal**: Install Hermes + MemSearch memory system
**Deliverables**: CLAUDE.md, context/MEMORY.md, context/USER.md, .claude/hooks/transcript-capture.js, .claude/settings.json
**Decisions**: Using `local` embedding provider (sentence-transformers) to avoid OpenAI API key dependency; KMP_DUPLICATE_LIB_OK=TRUE env var needed on macOS due to duplicate OpenMP runtime
**Open threads**: memsearch index is empty until daily logs accumulate — re-run `KMP_DUPLICATE_LIB_OK=TRUE memsearch index context/memory context/transcripts --provider local` after a few sessions
