<!-- Cap: 2,500 chars. Agent maintains via memory-write instructions. -->
# Working Memory

## Active Threads

## Environment Notes

- Stack: FastAPI (Python 3.12) + React 18 + Vite + TypeScript + TailwindCSS + PostgreSQL 16 + Redis + Celery + texlive Docker sidecar
- Local stack: `docker compose up` at http://localhost:3000 (frontend) / http://localhost:8000 (API)
- Admin account: admin@sudomoon.com (bootstrapped from ADMIN_EMAIL + ADMIN_PASSWORD in .env at startup)
- Primary user: sudhanshu@sudomoon.com
- Git remote: git@github.com:mightyjoe781/jobcraft.git on branch main
- Tasks/PRDs live in /tasks/ — always check before implementing a feature

## Pending Decisions
