<!-- Cap: 1,375 chars. Updated by the agent when it learns preferences. -->
# User Profile

## About

- Sudhanshu Kashyap (smk) — operator and primary user of JobCraft
- Building a self-hosted job tracking + resume tailoring workspace for ~50 trusted users
- Comfort level: technical, can read code, prefers concise explanations over hand-holding

## Preferences

- Commit after every meaningful change; use caveman-commit style (terse, factual)
- No emojis in code or commit messages unless explicitly asked
- Prefers light theme UI, minimalist design, no generic AI aesthetics
- Approach: fix bugs immediately, don't defer; implement features completely, not partially
- Response style: short and direct — no trailing summaries, no "here's what I did" narration
- PRDs live in /tasks/ — create one before implementing any significant feature
- Use subagents for large parallel work (theme migrations, multi-file refactors)

## Working Style

- Iterates fast — expects issues to be caught and fixed in the same session
- Uses /loop for implementation sprints, /prd before designing features
- Prefers self-contained Docker Compose stack; no external infra dependencies
